import {
  BlockNoteEditor,
  Block,
} from "@blocknote/core";

// Type for a block in our editor - using the default Block type
export type OurBlock = Block;

// Type for the editor instance - using the default BlockNoteEditor type
export type OurEditor = BlockNoteEditor;
